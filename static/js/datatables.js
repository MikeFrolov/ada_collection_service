$("#example").DataTable({
    //Datatables configurations
    paging: true, // Pagination
    pageLength: 20, // Data per page
    lengthChange: true, // Show entries per page
    autoWidth: true, // Control the auto width on columns
    searching: false, // Input search panel
    bInfo: false, // Show "How muth rows in this table" in footer
    bSort: true, // Sorted A to Z and Z to A(and numbers)
});